# Preview Fixture

This small README-style fixture checks common Markdown elements for local profiling.

- Render a compact document with predictable content.

Use the [project website](https://example.com) link and an inline `PreviewRequest` code span.

> Keep the fixture public, small, and repeatable.

```swift
let title = "Quick Look"
print("Rendering \(title)")
```
